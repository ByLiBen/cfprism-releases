# cfprism v1.1.2

本版本为优选软件增加网站账号登录与订阅鉴权。

### 主要更新

- 必须使用网站账号登录，只有有效订阅用户可以使用软件
- 同一账号仅允许一个软件会话，另一端登录后旧会话自动失效
- 软件前端和 Go 后端同时强制校验登录状态
- 密码修改、订阅过期或服务端撤销会话后，软件会自动停止核心任务
- 登录令牌仅保存在本次运行内，关闭软件后需要重新登录
- Windows 离线包不包含产品源码、Git 元数据或构建缓存
- 保持自动更新资产名称和 SHA-256 校验流程兼容

### 使用要求

1. 从本 Release 下载 `cfbest-sub-gui-offline-windows-amd64.zip`
2. 完整解压 ZIP 后运行 `cfbest-sub-gui.exe`
3. 使用网站账号登录，并确保账号订阅处于有效状态
4. 同一账号在其他设备登录后，当前设备会被自动退出

### 迁移说明

`v1.1.0` 及更早版本仍使用旧更新地址，无法自动发现新发布仓库。首次使用本系列新版本时需要手动下载并安装；安装 `v1.1.2` 后，后续版本可通过软件内更新检查发现。

### 下载资产

资产名称：`cfbest-sub-gui-offline-windows-amd64.zip`

SHA-256：

```text
3ac5b8984586bea0d7d8e435226e86b3f68ace267b44b4e61db5a96491ba56a2
```
